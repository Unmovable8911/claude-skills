## Project Info

- **A collection of agent skills, continuously expanded for diverse use cases.**

## Constraints

- Always reply in simplified Chinese, regardless of user's input language.
- Write or modify files only in English.
- When writing a new skill or modifing a skill, only read the files within the skill's directory user specified, you don't need to understand other things, DO NOT try to read other files.

## Requirements

After adding a new skill:

- Update `README.md` and `skills.json` following existing patterns.